# Boltz example

`protein.yaml` is a minimal single-chain input in Boltz's expected format.

Once `moldesk install boltz` and `moldesk run` are implemented:

```bash
moldesk install boltz
moldesk run boltz examples/boltz/protein.yaml
```
