[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "moldesk"
version = "0.0.1"
description = "Python-side helpers for MoleculeDesk model runners (not yet published)"
readme = "README.md"
license = { text = "MIT" }
requires-python = ">=3.10"

[project.urls]
Homepage = "https://github.com/pooriyapfn/moldesk"
